image: node:latest

stages:
  - install
  - test

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: install
  script:
    - cd /Code/
    - npm install
    - npm install dotenv

test:
  stage: test
  script:
    - cd /Code/
    - npm run test
